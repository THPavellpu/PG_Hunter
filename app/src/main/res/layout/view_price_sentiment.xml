<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clipToOutline="true"
    android:background="@drawable/bg_round_corners"
    android:elevation="6dp"
    >

    <!-- Background Image -->
    <ImageView
        android:id="@+id/bgImage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/bgofai1"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"
        android:background="@drawable/bg_round_corners"
        android:clipToOutline="true" />

    <!-- Foreground Content -->
    <LinearLayout
        android:id="@+id/priceSentimentLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingVertical="12dp"
        android:paddingHorizontal="16dp"
        android:gravity="center_vertical"
        >

        <!-- Title Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingBottom="8dp"
            android:paddingTop="14dp">

            <ImageView
                android:id="@+id/ivAiIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ai"
                android:contentDescription="AI Icon"
                app:tint="@color/ofwhite" />

            <TextView
                android:id="@+id/tvPriceSentimentTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="AI Price Analysis"
                android:textStyle="bold"
                android:textSize="17sp"
                android:textColor="@color/ofwhite"
                android:layout_marginStart="8dp" />
        </LinearLayout>

        <!-- Gradient Bar -->
        <FrameLayout
            android:id="@+id/barContainer"
            android:layout_width="match_parent"
            android:layout_height="22dp"
            android:background="@drawable/price_gradient"
            android:paddingHorizontal="4dp"
            android:layout_marginBottom="6dp">

            <View
                android:id="@+id/indicator"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_gravity="start|center_vertical"
                android:background="@drawable/indicator_circle" />
        </FrameLayout>

        <!-- Labels -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingBottom="4dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Bad Deal"
                android:textSize="12sp"

                android:textColor="@color/ofwhite"
                android:gravity="start" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Fair Price"
                android:textSize="12sp"
                android:textColor="@color/ofwhite"
                android:gravity="center" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Great Deal"
                android:textSize="12sp"
                android:textColor="@color/ofwhite"
                android:gravity="end" />
        </LinearLayout>

        <!-- Description -->
        <TextView
            android:id="@+id/tvPriceSentimentDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Fetching price insights..."
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/ofwhite"
            android:paddingTop="4dp" />
    </LinearLayout>
</FrameLayout>
